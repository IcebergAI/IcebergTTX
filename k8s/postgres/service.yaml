apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: deep-thought
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: deep-thought
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
